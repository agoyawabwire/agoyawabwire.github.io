<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PoultryFlowPro"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Register" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".DailyProductionActivity"
            android:exported="false"
            android:label="Daily Production"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FlockSettingsActivity"
            android:exported="false"
            android:label="Flock Settings"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ChickenSalesActivity"
            android:exported="false"
            android:label="Chicken Sales"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".EggSalesActivity"
            android:exported="false"
            android:label="Egg Sales"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ExpensesActivity"
            android:exported="false"
            android:label="Expenses"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>